---
title: "Threat Model"
description: "A high-level view of what the Rabit Contract trusts and what it defends on-chain."
---

## Main trust boundaries

| Boundary | What Rabit trusts | What the contract enforces |
| --- | --- | --- |
| user payment source | the user chooses the payment mint and token account path | profile ownership and delegate approval are explicit |
| delegated backend charging | the user chooses whether to trust the backend | delegation expiry and spending limit are enforced on-chain |
| admin governance | the admin controls fees and pause state | admin powers are explicit and separated from user payment sources |
| backend cost reporting | the backend reports `base_cost` and `service_cost` | the contract deterministically applies markup and platform fee |

## Core threats and mitigations

| Threat | Main mitigation |
| --- | --- |
| someone tries to charge from another user's token account | PDA derivation, owner checks, and token account ownership validation |
| backend keeps charging after delegation should be invalid | active flag, expiry checks, and user revocation |
| backend tries to exceed delegated budget | cumulative spend limit is checked on every delegated charge |
| admin tries to set extreme fees | hard-coded fee and markup caps |
| protocol discovers abnormal behavior in production | pause gate blocks new usage recording |
